pipeline:
  install:
    image: node
    commands:
      - node -v
      - npm -v
      - npm install

  test:
    image: node
    group: tests
    environment:
      CODECOV_TOKEN:
        from_secret: CODECOV_TOKEN
    commands:
      - npm test